{
  "name": "Server-API-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node tests/*.test.js | tap-spec",
    "test-api": "node tests/api.test.js | tap-spec",
    "test-logic": "node tests/logic.test.js | tap-spec",
    "test-server": "node tests/server.test.js | tap-spec",
    "start": "nodemon src/server.js",
    "coverage": "nyc tape tests/*.test.js | tap-spec",
    "report-coverage": "nyc report --reporter=text-lcov > coverage.lcov && codecov",
    "lint": "eslint ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "codecov": "^3.6.1",
    "eslint": "^6.7.1",
    "nock": "^11.7.0",
    "nodemon": "^2.0.1",
    "tap-spec": "^5.0.0",
    "tape": "^4.11.0"
  },
  "dependencies": {
    "nyc": "^14.1.1",
    "dotenv": "^8.2.0"
  }
}
